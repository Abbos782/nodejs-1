let name = 'Max'

let age = 24

let HasHobbies = "hello word"

function summarizeUers(name, age, HasHobbies){

    return (
        'Name is' + name + ', age is ' + age + 'and the user has hobbies:' + HasHobbies
    )
}

console.log(summarizeUers(name, age , HasHobbies))